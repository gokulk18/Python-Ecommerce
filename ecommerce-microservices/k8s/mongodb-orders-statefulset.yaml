apiVersion: v1
kind: Service
metadata:
  name: mongodb-orders
  namespace: ecommerce
spec:
  ports:
  - port: 27017
    targetPort: 27017
  clusterIP: None
  selector:
    app: mongodb-orders
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-orders
  namespace: ecommerce
spec:
  serviceName: "mongodb-orders"
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-orders
  template:
    metadata:
      labels:
        app: mongodb-orders
    spec:
      containers:
      - name: mongodb
        image: mongo:7
        ports:
        - containerPort: 27017
          name: mongo
        volumeMounts:
        - name: mongodb-orders-data
          mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: mongodb-orders-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
